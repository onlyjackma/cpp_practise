{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",  
 
            "type": "shell",   
 
            "command": ["./build.sh"],
 
           "args": [         
            ],
            "group": "build",
 
           "problemMatcher": [
                "$gcc"
            ]
        },

        {
            "label": "build-clean",

            "type": "shell",

            "command": ["rm build -rf"],
            
            "group": "build"
        },
    ]
}